export function Footer() {
  return (
    <footer className="relative z-10 border-t border-border/70 py-8">
      <div className="page-shell flex flex-col gap-3 text-sm text-muted-foreground sm:flex-row sm:items-center sm:justify-between">
        <p>ViaPathHub MVP for quick, trusted service matching in Tagum City.</p>
        <p>Built around convenience, verified talent, and flexible local jobs.</p>
      </div>
    </footer>
  );
}
